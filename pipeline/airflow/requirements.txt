# Dependencies baked into the custom Airflow image.
# Versions pinned to match the sgprimary conda environment (environment.yml).
#
# dbt-core pinned to 1.10.9 (matches local env) — dbt-bigquery 1.10.3 has no upper
# bound on dbt-core, so pip resolves to 2.0.x which has breaking schema.yml changes.
# google-cloud-storage pinned to 3.1.0 — dbt-bigquery 1.10.3 requires <3.2.0.

requests==2.32.5
beautifulsoup4==4.14.2
pyarrow
pandas==2.3.3
python-dotenv==1.1.0
google-cloud-bigquery==3.41.0
google-cloud-storage==3.1.0
dbt-core==1.10.9
dbt-bigquery==1.10.3
# ML training dependencies (ml_subscription_rate_training DAG)
# numpy and mlflow pinned exactly — prevents pip backtracking to mlflow 2.15.x
# which requires numpy<2.0 and would attempt a source build (no compiler in image)
numpy
lightgbm>=4.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
# mlflow==2.22.5
mlflow>=2.15.0,<3.0.0
google-cloud-bigquery-storage>=2.0.0
